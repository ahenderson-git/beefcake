# Cargo configuration for Beefcake
#
# Note: rustdocflags with --html-in-header requires the file to exist
# Uncomment the lines below if you want custom CSS in documentation:
#
# [build]
# rustdocflags = [
#     "--html-in-header", "doc-header.html",
# ]
